1.comparator 

Ans:A comparator provides multiple sorting sequences and it uses compare() method to sort the elements.

2. 
d.push("welcome");
        d.push("hllo");
        d.push("alpha");
        System.out.println(d);
        
        System.out.println(d.pop());
        System.out.println(d.pop());
		

Ans : d.pop() will remove the data in LIFI manner. 	

3.Ans :
Iterate a map using map.entry

for(Map.Entry o:m.entrySet()){
		
		System.out.println(o.getKey() +" "+o.getValue());
	}

4. can i add list into another list?
Ans:Yes, It can be done by using addAll() method.

5. can i add a list into set or map?
Ans:Yes, it can be done by using addAll() method.